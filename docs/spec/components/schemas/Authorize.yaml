allOf:
  - $ref: '#/components/schemas/AuthorizeKey'
  - type: object
    x-go-is-request: true
    required:
      - attributes
    properties:
      attributes:
        required:
          - proofs
        type: object
        properties:
          proof:
            $ref: '#/components/schemas/Proof'
