type: object
required:
  - id
  - type
properties:
  id:
    type: string
    example: "0x123...abc"
    pattern: '^0x[0-9a-fA-F]{64}$'
    description: User nullifier 32 bytes
  type:
    type: string
    enum: [ token ]
